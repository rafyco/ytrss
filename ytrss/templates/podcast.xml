<?xml version="1.0" encoding="utf-8"?>
<!-- generator="ytrss - http://github.com/rafyco/ytrss" -->
<rss version="2.0" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" >
    <channel>
        <title>{{ podcast_info.title }}</title>
        <itunes:author>{{ podcast_info.author }}</itunes:author>
        <language>{{ podcast_info.language }}</language>
        <link>{{ podcast_info.link | format_str }}</link>
        <description>{{ podcast_info.desc }}</description>
        <itunes:summary>{{ podcast_info.desc }}</itunes:summary>
        {% if podcast_info.img is not none %}
        <image>
            <title>{{ podcast_info.title }}</title>
            <url>{{ podcast_info.img | format_str }}</url>
            <link>{{ podcast_info.link | format_str }}</link>
        </image>
        {% endif %}
        {% for movie in movies %}
        <item>
            <title>{{ movie.title | format_str }}</title>
            <guid>{{ movie.identity }}</guid>
            <image>
                <title>cover</title>
                <url>{{ movie.image | format_str}}</url>
            </image>
            <link>{{ movie.url | format_str }}</link>
            <author>{{ movie.author | format_str }}</author>
            <itunes:author>{{ movie.author | format_str }}</itunes:author>
            <itunes:image href="{{ movie.image | format_str }}"/>
            <description><![CDATA[
            {{ movie.description | format_desc }}
            <br />
            <img style="width:100%" src="{{ movie.image }}" />

            <p>Uploader: {{ movie.author }}</p>
            <p>Site: <a href="{{ movie.url | format_str }}">{{ movie.url | format_str }}</a></p>
            {% if movie.resources_files | length %}
            <p>Resource files:
                <ul>
                    {% for file_path in movie.resources_files %}
                    <li><a href="{{ podcast_info.url_prefix }}/{{ file_path }}">{{ file_path }}</a></li>
                    {% endfor %}
                </ul>
            </p>
            {% endif %}
            <p>Podcast generated by: <a href="https://ytrss.readthedocs.io/">ytrss</a></p>
            ]]>
            </description>
            {% for file_path in movie.resources_files %}
            <enclosure url="{{ podcast_info.url_prefix }}/{{ file_path }}" type='{{ file_path | mimetype }}' />
            {% endfor %}
            <pubDate>{{ movie.date | format_date }}</pubDate>
        </item>
        {% endfor %}
    </channel>
</rss>
