---
name: Python package checks

on: # yamllint disable-line rule:truthy
  push:
    branches-ignore:
      - master
      - main

jobs:
  style:
    name: Checks code style
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install uv
        run: pip install uv
      - name: Check style
        run: uv run task style

  typing:
    name: Checks types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install uv
        run: pip install uv
      - name: Checks typing
        run: uv run task typing

  unittests:
    name: Runs unittests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        versions: ["3.11", "3.12"]
    needs:
      - style
      - typing
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python ${{ matrix.versions }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.versions }}
      - name: Install uv
        run: pip install uv
      - name: Test with pytest
        run: uv run task unittests

  documentation:
    name: Build documentation
    runs-on: ubuntu-latest
    needs:
      - style
      - typing
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install uv
        run: pip install uv
      - name: Build documentation
        run: uv run --group docs task documentation
      - name: Archive documentation
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: documentation
          path: build/sphinx/html/

  execute:
    name: Check terminal program
    runs-on: ubuntu-latest
    needs:
      - unittests
    strategy:
      fail-fast: false
      matrix:
        versions: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python ${{ matrix.versions }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.versions }}
      - name: Install uv
        run: pip install uv
      - name: Run console program
        run: uv run task execute

  analyze:
    name: CodeQL Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: "python"
      - name: Autobuild
        uses: github/codeql-action/autobuild@v2
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2
